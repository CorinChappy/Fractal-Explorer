import java.util.ArrayList;


class FractalCache<T extends Fractal> {
	private ArrayList<ArrayList<Integer>> cache;
	
	public int[][] dumpCache(){
		int[][] a = new int[cache.size()][cache.get(0).size()];
		for(int i=0;i<cache.size();i++){
			for(int j=0;i<cache.get(i).size();j++){
				
			}
		}
		
		
		return a;
	}
	
	//public over
	
	
}
